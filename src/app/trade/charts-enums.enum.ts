export enum Producttypes {
    Index = 0,
    NotIndex = 1
}

export enum PeriodType {
    Daily = 0
}

export enum ChartType {
    Line = 0,
    Area = 1,
    Bhlc = 2,
    Candles = 3
}

export enum ChartDisplay {
    Rate = 0,
    Precentage = 0
}

export enum ChartLinkage {
    Nominiak = 0,
    InDollar = 1
}